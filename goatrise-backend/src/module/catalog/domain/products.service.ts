import type { ItemAttributeValues } from "../../inventory/schema/items.schema.js";

export function checkIfItemsIsValidatedForProduct(attrValues: ItemAttributeValues[], productId: string): boolean {
  return true;
}